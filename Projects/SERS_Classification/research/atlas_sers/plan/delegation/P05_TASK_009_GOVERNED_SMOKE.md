# P05-T009 — governed private plan and smoke boundary

Supervisor-issued 2026-09-25 to OpenCode Go / DeepSeek v4.1 Flash. Return patches from supplied source snapshots with all tools denied. No scientific execution, direct writes, Git, or actual data access by the worker.

Allowed additions: `src/atlas_sers/evaluation/p05_core_run.py`, `scripts/run_p05_core.py`, `tests/test_p05_core_run.py`. Module and wrapper first, tests next. Reuse the reviewed planner and numerical kernel; do not change their protocol. Public export is a separate slice.

Provide `plan` and `smoke` subcommands with explicit private artifact root. Authenticate the exact contract, inherited P01/P04PLAN successful states/reports and pinned files; reject symlinks, untrusted paths, changed LATEST, malformed or duplicate identities. Run the inherited metadata validator. The immutable metadata-only plan is hash-identified and must exist before smoke; require explicit matching `--plan-id` and `--contract-sha256` for smoke. No future development or outer-evaluation command is exposed.

Use a separate `p05core` private namespace outside the repository, retaining JSON plan, input/code/environment hashes, execution ledger, all epoch histories, terminal CPU checkpoints and a path-free summary. Never modify older artifacts. One exclusive contract-level smoke lease is created before any fit, independent of code/run identity, so changing code cannot bypass the 34-execution cap. Existing running/failed/completed lease prevents new execution; no retries or quarantine-and-restart behavior. Reserve each of the 34 scheduled execution rows on disk before the fit starts. On failure retain all evidence and stop. A hard interruption leaves the consumed lease, not permission to retry.

Verify representation hash/axis/UID population and source-role membership. Only allowlisted fitting arrays and QC metadata enter the kernel or diagnostics; no held predictions, validation scores, model selection or calibration. The full NPZ container is physically opened only for extraction. Preserve the fixed 8 epochs, 4 draws, 32 primary fits and 2 predeclared replays. Reconcile exact execution/step counts and enforce per-fit 120-second, total 900-second and 4-GiB allocated CUDA bounds. Choose CUDA only with at least 5-GiB free before launch, otherwise CPU before the first fit; no mid-fit fallback. Set deterministic environment before importing torch.

At acceptance, require finite complete histories, expected parameters, finite nonzero backbone gradients and changed weights, nonzero enabled projection gradients, dense auxiliary availability, explicit sparse paired absence, identical common initial backbones/sampling/augmentation/pair streams across recipes, exact sparse D0-M/D2 and D1/D3 numerical equality, and both planned replays matching terminal states and semantic histories. Compare only numerical loss/history fields for sparse controls, since enabled flags correctly differ. Cost fields are not replay equality requirements. Eight-epoch training accuracy is diagnostic, never an advancement threshold.

Capture protected package/code/Git/environment identity before and after with stable identity fields, excluding volatile disk space/time. No concurrent package edits during execution. Successful completion requires unchanged inputs/protected state and all checks, with hashed output manifest. Failures must remain failures and cannot publish passing claims. Tests use synthetic fixtures and mocked boundaries where necessary; no real-data fitting in unit tests. CLI output contains only status, IDs and aggregate counts, no private identities or paths.
