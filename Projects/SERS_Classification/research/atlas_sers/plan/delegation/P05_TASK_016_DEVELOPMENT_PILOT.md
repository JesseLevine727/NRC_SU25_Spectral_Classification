# P05-T016 — approved 36-fit source-validation pilot runner

Owner approved the bounded 36-fit pilot on 2026-09-25: one preselected source fitting/validation unit per station, four recipes, three seeds, 30–200 epochs, at most 90 minutes and 4 GiB CUDA allocation. These are 36 of the already registered 14,940 inner slots, not additional hyperparameter trials. No retries, full ladder, outer evaluation, calibration or G3 selection is authorized. Preserve all original smoke/recovery evidence and contracts.

Worker: OpenCode Go / `opencode-go/deepseek-v4.1-flash`, all tools denied, public source snapshots supplied. Allowed new files: `src/atlas_sers/evaluation/p05_pilot.py`, `scripts/run_p05_pilot.py`, `tests/test_p05_pilot.py`. Return implementation first, tests separately. Do not edit other modules or contracts. Supervisor applies, corrects via worker, tests, commits, launches and checks. No real spectra or private metadata goes to the worker.

## Metadata preflight and permit

Authenticate `p05_development_pilot.json` against the independent canonical SHA supplied in the prompt; authenticate original core contract/plan with existing `_load_contract`, `_authenticate`, `_build_plan`, `_minimal_plan_checks`. Rebuild the development ledger with T015. Use one nonexcluded inherited unit per cwa/pills/surfaces from held-evaluation contexts: prefer pseudo_domain, otherwise master_cv; lexicographic `(context_id, selection_unit_id)` within that preference. Choice uses metadata only, never scores. Select the exact 12 registered slots of each unit and preserve their original slot IDs, fitting/validation role IDs and seeds. Record actual selected roles, exact UID sets/hashes and support privately before any fit. The pilot plan binds core/ledger/permit identities and all 36 ordered slots; CLI run requires its independently reviewed plan digest.

Provide `preflight` (metadata/hash/resource/serialization checks only; no lease, arrays or fits) and `run` operations. Artifact root must be outside the entire repository and not symlinked. Reuse existing root/path/symlink, input authentication, provenance and atomic checkpoint helpers. Use a new private `p05development` namespace; never call original smoke/recovery entrypoints. Do not fake authorization by changing original false flags: the separate pilot permit is the sole new execution authority. No arbitrary path or unregistered slot override.

## Exactly-once and resource boundary

One exclusive permit-level pilot lease and run directory; either existing prohibits relaunch. Additionally reserve a contract/plan-level slot lease for each original development slot immediately before its first optimization: future full development must reuse accepted pilot evidence, not refit the same consumed slot. Slot lease is independent of code or permit revisions. Fail closed if any selected slot was previously consumed, before starting a new pilot. Never remove failed leases, retry, reset, silently skip a partial run, or exceed 36 executions/28800 updates. Distinguish started/completed/failed/unstarted and actual updates. Bind provenance (code/Git/dependencies/compute/inputs), keep package fixed while executing, and check protected identity before acceptance. Record partial histories/errors/checkpoints on failure and stop; do not proceed to another fit after any failure.

Enforce 120 seconds per fit, 5400 seconds overall, 4 GiB allocated CUDA, at least 5 GiB free before selecting CUDA, 2 GiB private pilot output cap, and enough free disk before lease. Device choice fixed before first fit, no automatic CPU fallback midrun. Per-fit kernel receives finite remaining global deadline. Supervisor supplies external process watchdog. Reuse actual checkpoint preflight before lease and re-load/hash every saved best and terminal checkpoint; test real serialization, not only mocks. Use the original exact core hyperparameters; source-validation stopping is kernel-owned.

## Data and output handling

After preflight and exclusive pilot lease, verify frozen representation SHA and UID/axis alignment using `_load_representation`; extract only selected fitting/validation rows. Fitting QC columns from pinned P01 `primary_manifest.csv` via `_noise_frame` must match exact fitting UID order. Build Observation objects from authenticated source manifest; never use outer-test intensities, predictions, targets for scoring, noise estimation or stopping. The container must be opened, but only allowlisted arrays enter fit/evaluation. T015 establishes source exclusions; reassert fit/validation/outer boundaries before calling the kernel. Keep a canonical private input-order manifest.

Use `train_development_fit` exactly; `role_id` is its registered fitting role ID (not recipe/slot-dependent). Save all epoch diagnostics, summary result, ordered validation logits/classes/UIDs, best and terminal states, parameter counts, hashes and actual counts. Array/logit files, identities and checkpoints stay private. Persist an artifact hash manifest. Public summary is a strict aggregate whitelist (station, recipe, seed counts, completion/epoch/update/resource counts and claim boundary), not a dump of private dataclasses or errors. Inspect finite histories, expected 30..200 epochs, 4 updates/epoch, best epoch within history, exact parameter counts, restored-best logits/metrics identity and matching shared RNG prefixes across recipes for the same unit/seed. Sparse reductions are checked where a unit lacks pairs; no positive-support requirement may discard a sparse role.

## Tests and stop

Synthetic fixtures test metadata-only preflight, exact 36-slot selection, role preference/ties, permit/core/plan tampering, path/lease/slot refusal, no reuse under changed code, empty/foreign/misaligned arrays, real saver roundtrip, failure stopping subsequent fits and retaining counts/history/checkpoints, all 36 mocked results through orchestration, counters and shared-prefix checks, resource refusal before lease/optimization, and original artifact preservation. Test module imports without torch when possible; explicitly skip only actual neural/serialization checks without torch.

Stop after implementation/tests. Real pilot launch requires supervisor independent review and committed implementation. A successful pilot still does not authorize expansion or a performance-selected recipe; a separate figure task will export training/source-validation diagnostics in native TikZ and offline HTML.
