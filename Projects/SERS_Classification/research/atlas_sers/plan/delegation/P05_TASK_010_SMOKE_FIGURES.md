# P05-T010 — training-only smoke figures

Supervisor-issued 2026-09-25 to DeepSeek via OpenCode Go. Return scoped patches only; no tools, data access or execution. Allowed additions: src/atlas_sers/visualization/p05_smoke_figures.py and tests/test_p05_smoke_figures.py. First deliver module, then tests. Do not alter frozen registries.

Produce native pgfplots/TikZ and standalone offline Plotly HTML from one allowlisted semantic table; also compile vector PDF and PNG using unchanged P04 helpers. Two figures, each 2x2 source-role panels: (1) weighted chemical training CE versus epochs1–8; (2) pre-clipping gradient norm versus epochs. Four recipes D0-M,D1,D2,D3, two seeds displayed as seed1/seed2 with solid/dashed lines and dots. Every point is one fit-epoch, not an independent sample; do not average seeds or include replays as extra evidence. Legends explain D0-M ordinary classification, D1 chemical similarity, D2 matched-sample consistency, D3 both. No bar charts. Use colorblind-safe colors, black text, readable labels. Label these eight-epoch numerical checks, not model comparisons on unseen data.

API build_semantic_data(records) accepts 34 execution summaries containing schedule keys plus kernel history; retains only the 32 primary fits, whitelists role_label,recipe_id,seed_index,epoch,chemical_ce,gradient_norm_mean. Strictly require all four known roles, four recipes, two known seeds, each epoch1–8 exactly once, finite nonnegative numeric values; reject missing/duplicate/truncated fits. Output dataframe256rows in deterministic order. Never copy IDs, UIDs, masters, paths, raw spectra or prediction rows to semantic data, labels or hover. Reject malformed record types. Export API generate_smoke_figures(records,output_root) returns manifest with relativepaths/hashes. Fresh output directory required, no overwrite, no symlinks. Use data.csv sha in both formats. Table itself contains only whitelisted fields. Public files semanticCSV,tex,html,pdf,png,figuremanifestJSON; logs outside/removed by sharedhelper. Use fixed filenames P05S01_training_ce and P05S02_gradient_norm.

Sparse surface panel must state that matched-instrument pairs are unavailable and D2/D0-M and D3/D1 overlap by construction; overlap is not evidence of learned invariance. Support counts13,9,13,4masters can appear in titles, but no identities. Replays are reproducibility checks and excluded from plots. No held BA, no winner, no generalization claims. Deterministic publication, suppressPDFdates. Tests synthetic full34recordfixtures: filtering replays, schema/privacy, duplicate/missing/nonfinite rejection, deterministic row ordering, valid native pgfplots source and same-data hash in HTML. No real-data training. Do not need productionPDFcompile in everyunit test; supervisor separately compiles/inspects actualfigures.

