# P05-T013 — source-validation training kernel

Supervisor assignment, 2026-09-25. The owner requested proceeding with the listed P05 configuration, with OpenCode Go / `opencode-go/deepseek-v4.1-flash` implementing and the supervisor reviewing/correcting. This slice implements and synthetically tests the next training kernel. It does not launch the full dataset schedule or change the completed smoke contract, implementation, evidence or lease.

## Worker boundary

Return complete `apply_patch` patches from supplied public source snapshots. All worker tools, direct edits, actual data access, Git, additional agents and execution are denied. Allowed new files: `src/atlas_sers/evaluation/p05_development.py` and `tests/test_p05_development.py`. No other implementation changes. Deliver the module first; tests are a separate requested continuation. Never claim unexecuted tests passed.

## Required interface and behavior

Implement an in-memory `train_development_fit` returning a dataclass. Inputs: fitting `values`, `observations`, `noise_metadata`; `validation_values`, `validation_observations`; `role_id`, `recipe`, `seed`, `device`; explicit positive `maximum_fit_seconds` bounded by 120, optional global deadline and CUDA cap no larger than 4 GiB; optional epoch callback. No filesystem reader/writer, private loader, CLI, calibration, outer predictions, candidate selection or scientific launch authority.

Production training is exactly minimum 30 / maximum 200 epochs, patience 20, four batches per epoch. Do not expose reduced epoch schedules in the production API to make tests convenient. Tests may mock epoch evaluation to exercise stopping with the real compact model. Best checkpoint ordering is validation balanced accuracy descending, NLL ascending, earliest epoch. Minimum 30 means minimum training duration, not minimum best-checkpoint epoch. Strict lexicographic improvement resets patience; equality does not. Reuse P04 per-spectrum validation metrics unchanged, explicitly distinct from equal-master loss weighting. Best-checkpoint logits must be recomputed after restoring the best state and returned aligned with canonical validation UID order; retain terminal-state hash separately.

Reuse immutable architecture, sampler, objectives, augmentation and deterministic helpers. Preserve P05 smoke recipe-independent role/seed initialization, batch/dropout and augmentation RNG stream conventions. Do not edit or monkeypatch the smoke's eight-epoch guard. Its pure input-validation helpers may be reused for structural checks; the development duration is independently bounded. Check exact parameter counts, finite loss/gradients/weights/logits/metrics, global gradient clipping 5, AdamW 0.0003/0.0001, and no LR schedule. Use no validation data to estimate augmentation or noise. Record loss components/support, training and validation metrics, norms/clipping, per-epoch and total optimizer updates, state/stream hashes, best epoch, collapse (<2 predicted validation classes at best checkpoint), elapsed time and peak memory. When candidate training stops earlier, matched streams mean identical draws on the shared epoch prefix, not equal final full-run digests.

Validate finite float32 min-max input arrays (N,1401), sorted unique UID alignment, three fitting/validation classes with identical vocabulary, common station, internally consistent master labels, and zero fitting/validation UID or physical-master overlap before optimization. Validate fitting noise metadata exactly as smoke. Copy input arrays/metadata as needed; do not mutate caller data. Preflight sampler capacity before fitting. Resource limits apply around setup, each update and all evaluation/final prediction work, including immediately after final work; synchronize CUDA before timing checks. No mid-run device fallback or retries.

For an operational failure, retain partial history, actual consumed updates, initial/best/terminal state information where available, sanitized reason and traceback digest; never call a failed fit complete. Callback failures must also stop the fit with retained state. Data/config validation may raise before optimization. Return CPU-cloned checkpoint state, classes and ordered validation UIDs with logits only on success; no misleading validation predictions on failure. Avoid retaining per-epoch model copies or GPU tensors.

## Review tests

Synthetic-only tests: all four recipes and exact parameter counts; minimum 30, maximum 200, patience/tie ordering and restoring a pre-30 best checkpoint; repeatable histories/state and matching RNG prefixes; single-instrument D0-M/D2 and D1/D3 equivalence; sparse support accounting; UID/master overlap, station/vocabulary/alignment/range/NaN rejection before optimization; input immutability; source-only noise derivation; expiry/memory/nonfinite/callback failures preserving histories and counts; best-state real serialization roundtrip using the existing checked saver. Hosted environments without torch explicitly skip neural tests rather than claiming neural validation.

Stop after module/tests and corrections. Dataset execution remains behind a separate reviewed runner and concrete resource gate.
