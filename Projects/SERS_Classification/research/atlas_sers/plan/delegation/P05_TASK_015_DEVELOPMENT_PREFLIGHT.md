# P05-T015 — executable source-role ledger, no training

Supervisor assignment, 2026-09-25, to OpenCode Go / `opencode-go/deepseek-v4.1-flash`. All worker tools/direct edits/data/Git/execution denied. Return complete scoped patches. Allowed new files: `src/atlas_sers/evaluation/p05_development_plan.py`, `scripts/inspect_p05_development.py`, `tests/test_p05_development_plan.py`. Module and CLI first; tests on a requested continuation. Do not modify inherited core plan or smoke code.

## Purpose

Convert the existing immutable plan's 14,940 development slots and source-master guard memberships into an independently checked executable metadata ledger. This stage reads authenticated metadata and hashes, not intensities, predictions or training outcomes. It supplies the forthcoming runner with explicit, validated source fitting/validation rows. It does not authorize any fit, lease, checkpoint, calibration or test prediction.

Implement pure `build_development_ledger(*, plan, support, contract)` and read-only CLI using existing core-run `_repository_root`, `_assert_artifact_location`, `_load_contract`, `_authenticate`, `_build_plan`, `_minimal_plan_checks`; default contract SHA is the pinned original core digest. Authenticate/rebuild plan without `_store_plan` (no writes); compare canonical digest against the pinned existing plan ID `a6334b2ed13a92fd953e4202bc2153e1aea4d12419d2a6f891f64f126136fe37`. Public CLI stdout is an aggregate whitelist only. Do not leak private IDs, file paths, samples, observation identities, exception text or tables. No new dependencies/torch/NumPy import. CLI requires explicit `--project-root`, `--artifact-root`, `--contract`; exceptions sanitized to stable error codes/nonzero exit. Support module's authentication is inherited; real support audit is re-run by `_build_plan`.

Map inherited role IDs to their actual canonical UIDs, guarded by context/unit/role identities. Guard role definitions use `fitting_uids` for BOTH fitting and validation (the key is legacy naming, not meaning). Build one unit record per (context_id,selection_unit_id), not per seed/recipe: role IDs, canonical UID lists/digests, station, selection mode, source instrument sets, class counts, master counts, sampling capacity, exclusion status/reason. Keep explicit slots in ledger with referential integrity and require four recipes x three seeds per unit. Reject duplicate or unknown slots/units/contexts/roles, malformed flags, mismatched guard folds/reasons, stale UIDs/hashes, swapped fitting/validation roles, incomplete seed or recipe products and mismatched canonical slot identities. Bind ledger identity to source pins, immutable plan hash and all semantic records; order must be canonical and shuffle invariant.

For each nonexcluded unit, require nonempty fitting and validation, all three station-local classes, no UID or physical-master overlap, every row and master from that context's outer_fit source, and no outer-test master or outer held instrument in EITHER role. For pseudo-domain inherited units require validation on the registered pseudo-instrument (unit ID prefix `pseudo:`), and exclude that instrument from fitting; master-only and guard units do not invent instrument holdouts. Check same station and metadata consistency. Maximum all-master two-instrument sampling capacity <=48. Classify auxiliary support separately (cross-instrument master pairs; same-chemical positives); absence is not an exclusion or license to relax splits. Validate excluded guards' identities/membership/support against existing registry rather than silently skipping them.

Return private ledger plus aggregate summary with contexts by mode, inherited/guard unit counts, eligible/excluded slot counts, maximum masters/rows/capacity, minimum/maximum scheduled optimizer updates (30*4 or 200*4 per eligible fit; maxima are ceilings and minima assume completion), memory-neutral checkpoint payload estimate from exact parameter counts (label this a tensor-only floor, not total disk allocation), and explicit `execution_authorized=false`, `arrays_loaded=false`, `fits_started=0`, `outer_evaluation_authorized=false`. Expected aggregate counts remain 320 contexts, 861 inherited +384 guard units and14940 slots; derive and reconcile with contract, do not replace validation with hardcoded reports.

Tests: fixture with pseudo, inner_master_cv/master_cv and guard units; exact counts/arithmetic; row/table shuffles produce identical ledger; unchanged input; unknown/duplicate/foreign/mislabeled roles; UID/master overlap; held/pseudo instrument leakage; absent classes; unknown UIDs; guard UID digest corruption; incomplete ladder; excluded-slot consistency; CLI no-load/no-fit/read-only and sanitized failure output. Small fixtures may pass a contract-shaped count expectation to pure builder but CLI always authenticates original pinned contract/plan. Stop at metadata-only readiness; runner/pilot/resource authorization remains a separate task.
