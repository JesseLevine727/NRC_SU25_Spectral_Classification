# P05-T017 — aggregate pilot figures

Implementer: OpenCode Go / `opencode-go/deepseek-v4.1-flash`; all tools denied, public source snapshots only. Allowed new module `src/atlas_sers/visualization/p05_pilot_figures.py` and synthetic tests `tests/test_p05_pilot_figures.py`. Return the module first, tests separately. No execution, data access, model selection or edits to existing exporters.

Implement `build_semantic_data(records)` and `generate_pilot_figures(records, output_root)`. Each private input record combines station, recipe_id, seed, status, best_epoch, and the complete kernel history. Require exactly 36 complete unique station/recipe/seed records (three stations, four locked recipes, seeds 20260805/20260817/20260829), contiguous epochs starting at one, 30–200 completed epochs, and a best_epoch present in that history. Reject malformed/nonfinite/out-of-range metrics and counters rather than silently omitting data. Public output is a strict allowlist: station, recipe_id, seed_index, epoch, is_best, chemical_ce, train_balanced_accuracy, validation_balanced_accuracy, validation_nll, validation_macro_f1. No paths, observations, masters, instrument identities, raw spectra or row predictions. Keep all seed trajectories; stopping points may differ. Do not extend, impute, or silently average truncated trajectories.

Produce two figures from this one semantic CSV:

1. `P05P01_learning_curves`: three station rows and two columns (weighted chemical training CE; source-validation balanced accuracy). Twelve trajectories per panel, recipe colors from the existing smoke exporter, three distinct seed dashes, black labels, normal LaTeX font. Keep legends and captions readable. BA axes cover [0,1]; CE axes start at zero. Source-validation BA is not the training loss and is not an outer-test result.
2. `P05P02_best_checkpoints`: three station panels, scatter of unaugmented training BA (x) against source-validation BA (y) at each fit's selected checkpoint. Four recipe colors and three seed symbols; [0,1] axes with identity reference. Hover includes best epoch, source-validation NLL and macro-F1, but no private identifiers. All 12 points per station are retained even when overlapping.

Native pgfplots/TikZ `.tex`, compiled vector PDF, PNG preview and self-contained offline Plotly HTML are required for both. Reuse `_compile`, `_write_html`, `_tex` and immutable smoke palette/helpers where applicable. Same semantic data SHA in HTML and TikZ, a file/hash manifest, exclusive output directory, no source overwrites. Importable without torch. Generated TeX may not embed a raster figure.

Caption limits: one metadata-preselected source split per station, three initialization seeds, no G3 decision and no definitive generalization claim. CWA and surfaces validate on a source pseudo-instrument; pills uses source-master CV because the inherited station contexts lack pseudo-domain support. Seeds are not independent dataset splits. Show individual trajectories/points; do not manufacture confidence intervals or significance. This applies scientific-visualization guidance through colorblind-friendly colors, redundant dashes/symbols, readable black labels and matched semantic exports, retaining the user's native TikZ requirement.

Tests cover exact 36-fit population, three seeds, variable stops/pre-30 best epoch, ordering invariance, no input mutation, invalid/missing/duplicate data, semantic privacy allowlist, matching native coordinates/HTML data, exclusivity, manifest and a real compile smoke when available. Do not claim tests were run. Supervisor performs independent checks and visual review after the actual pilot is accepted.
