# P05-T012 — one bounded checkpoint recovery

Owner approval received 2026-09-25: preserve the original failed attempt; replay its completed numerical fit once with corrected persistence; then execute the 33 previously unstarted scheduled fits. Total ceiling: 35 executions / 1,120 optimizer updates including the failed-save attempt. No automatic retry and no broader benchmark authority.

The original contract and metadata plan MUST NOT change: their identities enter random streams. The separate recovery permit in `plan/contracts/p05_checkpoint_recovery.json` authorizes infrastructure recovery only. Its canonical hash `01e0835d8a6ece2ee98cee654e9f707e894643788dc9daabc438527c1c19c058` must be pinned in the recovery runner. The original failed lease must remain failed and byte-for-byte unchanged. The supervisor corrected the draft permit's missing project protocol namespace before any recovery launch; the original scientific contract is unaffected.

## Delegated implementation

OpenCode Go / DeepSeek v4.1 Flash authors patches, all tools denied. Supervisor supplies approved source snapshots, reviews and independently tests. Allowed new files: `src/atlas_sers/evaluation/p05_recovery.py`, `scripts/recover_p05_smoke.py`, `tests/test_p05_recovery.py`. Reuse unchanged numerical kernel and existing runner authentication, extraction, serialization, per-fit and aggregate acceptance helpers. Do not duplicate numerical training.

Authenticate permit, original six-file manifest and complete file inventory, failed lease, one-started/zero-completed ledger, original result and full history, and all numerical source pins. Reject symlinks/path traversal and unexpected original checkpoints. Compare original runtime, compute, dependencies and total filesystem size with recovery provenance; repository code may differ for the explicitly reviewed infrastructure fix, but numerical source hashes must not. Authenticate original immutable metadata plan and pinned upstream inputs afresh. Checkpoint preflight precedes a new exclusive lease.

Use a separate private prefix `p05core/recovery/<permit-sha>/` with `lease/` and `run/`. Existing recovery lease or run prohibits another attempt, even if failed. First scheduled fit retains all numerical identities but has a new deterministic execution ID and explicit recovery-of record. Remaining 33 plan records are unchanged. Check the first recovered result against original state/stream digests and full semantic history immediately, before allowing another fit. Retain the corrected checkpoint and result even if comparison fails. Persist a separate recovery link; do not alter original evidence.

Each new execution is reserved before optimization, journaled, saved using real torch serialization, reloaded on CPU and checked against the recorded final state hash. Fail closed on any invalid result, serialization, comparison or resource limit. Preserve error diagnostics and partial artifacts. New run has exactly 34 records (32 primary plus two planned replays); overall accounting includes original one / 32 updates. Enforce original 120-second per fit, 900-second new run, 4-GiB CUDA limits. Recheck original evidence, upstream inputs and within-run protected provenance before acceptance.

Tests must cover permit/source/original-evidence tampering, exclusive lease refusal, schedule identity, replay mismatch preventing later fits, budget accounting, real serialization roundtrip, all 34 synthetic result records through orchestration, sparse equivalence, original directory preservation and failure persistence. No actual private data is supplied to the worker. Torch tests skip explicitly if unavailable; hosted CI is not neural-validation evidence.

The full scientific smoke may start only after supervisor review, focused and full tests, public-artifact validation and a committed implementation. No package/Git changes during tests or governed execution. Once accepted, publish actual training-only curves in native TikZ, offline HTML, PDF and PNG; no predictive/generalization claim. The original attempt remains documented separately.
